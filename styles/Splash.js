import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  img: {
    width: '100%',
    height: '100%',
  },

  load: {
    position: 'absolute',
    top: '60%',
    alignSelf: 'center',
  },
});

export {styles};
